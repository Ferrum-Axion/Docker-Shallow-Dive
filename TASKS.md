# Tasks

I installed the required tools, set up an SSH server in Docker, and learned to run it with `sshd -D`. I created the Docker and Compose files, tested both standalone and compose runs, confirmed SSH access to the container, added a simple `setup.sh`, and documented everything in `README.md`.
